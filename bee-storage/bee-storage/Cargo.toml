[package]
name = "bee-storage"
version = "0.9.0"
authors = [ "IOTA Stiftung" ]
edition = "2021"
description = "A general purpose storage backend crate with key value abstraction API."
readme = "README.md"
repository = "https://github.com/iotaledger/bee"
license = "Apache-2.0"
keywords = [ "iota", "tangle", "bee", "framework", "storage" ]
homepage = "https://www.iota.org"

[dependencies]
packable = { version = "0.1.0", default-features = false }
serde = { version = "1.0.13", default-features = false, features = [ "derive" ] }
