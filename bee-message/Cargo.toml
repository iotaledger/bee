[package]
name = "bee-message"
version = "0.1.3"
authors = [ "IOTA Stiftung" ]
edition = "2018"
description = "Implementation of the IOTA protocol message layouts"
readme = "README.md"
repository = "https://github.com/iotaledger/bee"
license = "Apache-2.0"
keywords = [ "iota", "tangle", "bee", "framework", "message" ]
homepage = "https://www.iota.org"

[features]
serde1 = [ "serde", "serde-big-array" ]

[dependencies]
bee-ord = { path = "../bee-common/bee-ord" }
bee-packable = { path = "../bee-common/bee-packable", features = ["serde"] }
bee-packable-derive = { path = "../bee-common/bee-packable-derive" }

bech32 = { version = "0.8", default-features = false }
bitvec = { version = "0.20", default-features = false }
digest = { version = "0.9", default-features = false }
hashbrown = { version = "0.11", default-features = false, features = [ "ahash-compile-time-rng" ] }
hex = { version = "0.4", default-features = false }
iota-crypto = { version = "0.7.0", features = [ "ed25519", "blake2b" ] }
serde = { version = "1.0", features = [ "derive" ], optional = true }
serde-big-array = { version = "0.3", features = [ "const-generics" ], optional = true }

[dev-dependencies]
bee-test = { path = "../bee-test" }

serde_json = "1.0"
